import { pageMetadata } from '@/app/lib/page-metadata';

export const metadata = pageMetadata('/mof-sankey');

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
