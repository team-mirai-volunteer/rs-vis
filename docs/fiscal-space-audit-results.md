# 財政余力の再現条件と感度（2026-09-16.2）

この文書は `scripts/generate-fiscal-audit-report.ts` が生成する出力記録です。再生成：`npx tsx scripts/generate-fiscal-audit-report.ts`。係数の妥当性の証明・信頼区間ではありません。

条件：latest、社会保険料5・研究3・送電網3・防衛2・保育2兆円、その他は画面の既定値。年1〜5、IO負荷あり、税収弾性値1.1。現在の入力一式は [JSON](fiscal-space-audit-results.json)。画面はGDPギャップを小数1桁（%）に丸めており、初期状態を直接渡す試算とは探索刻み以内の差が生じ得ます。

## 基準インフレの感度

CPI上限2.5%と年0の観測値を固定。基準インフレだけを変更して全制約を探索します。

| 基準インフレ | 探索上限（兆円/年） | 状態 |
| --- | ---: | --- |
| 1.0% | 27.4 | boundary |
| 1.5% | 27.4 | boundary |
| 1.8% | 24.0 | boundary |
| 2.0% | 17.0 | boundary |
| 2.2% | 10.1 | boundary |
| 2.5% | 0.0 | baseline-violated |

## CPI上限の感度

基準インフレ2.0%。20%の任意控除は算術上の設定で、ストレスから推定した緊急予備費ではありません。主表示は控除前の単一値と上の感度表です。

| CPI上限 | 探索上限（兆円/年） | 任意控除後（兆円/年） | 境界・状態 |
| --- | ---: | ---: | --- |
| 2.0% | 0.0 | 0.0 | 物価 / baseline-violated |
| 2.5% | 17.0 | 13.6 | 物価 / boundary |
| 3.0% | 27.4 | 22.0 | 産業別能力 / boundary |
| 3.5% | 27.4 | 22.0 | 産業別能力 / boundary |

IO負荷なし・CPI上限3.0%は **unevaluated（算出不可）**。未評価制約は探索を停止させます。2024年で揃えた同配分は 16.0兆円/年。観測された年0 CPIは2.7%のまま保持し、違反判定から除外します。将来の政策なし経路が上限を超える場合は引き続き baseline-violated です。

## 税収弾性値の感度

入力予算15兆円を固定した年5の経路。弾性値は社会保険料を含む収入全体に複利で適用する仮定です。

| 弾性値 | 総債務/GDP | 税・社会負担収入（兆円） |
| --- | ---: | ---: |
| 1 | 186.4% | 241.1 |
| 1.1 | 184.8% | 245.0 |
| 1.7 | 174.5% | 269.5 |

既定の配分では物価が境界を決めています。債務の上限を見つけた結果ではありません。280%の閾値に実証的な持続可能性の根拠はなく、5年の評価は長期債務持続性の検証を代替しません。

## 2026-09-16.5 の変更点

- 労働制約を「構造的失業率÷失業率」に置き換え（既定 u* 2.5%、許容失業率下限2.0%）。15兆円例の参考上限は変わらず、控除前16.79・控除後13.43兆円（物価拘束）。
- 乗数倍率が公表反応全体に掛かるようになり、0.5では控除前27.45兆円（産業別能力拘束）、2.0では8.45兆円。
- 物価判定に「評価期間の平均」方式を追加。既定は単年ピーク。平均方式では15兆円例は産業別能力が先に拘束し27.45兆円。
- 5年評価の実現供給便益を政策比較表に列として表示（非資本の供給経路は設計上0）。
- 表示：留保後の参考上限を主表示、閾値の入力範囲を画面とURLで共用（範囲外はクリップして通知）、共有元のモデル版と補完項目を表示、未評価制約もメーターに斜線で表示、グラフは政策なしとの差の0起点、単一色相。

## 2026-09-16.6：任意控除をストレス控除に置換

「探索額×(1−20%)」の定率控除は、20%に出典がなく主表示を根拠なく動かしていたため廃止した（`reserveShare` は0に固定）。参考上限は、利用者が選んだストレスすべてに耐える額（各ストレスで同じ配分を再探索した最小値）とし、実質的な控除率は結果として表示する。4条件（許容インフレ−0.3pt、輸入物価+3%、輸入エネルギー+20%、借換金利+100bp）の額を常に計算して表示し、既定では未選択（探索額をそのまま表示）。CPI上限2.5%・政策なしCPI約2.05%では、モデルの経路は輸入物価→CPI（転嫁0.13）だけで円安はその一因にすぎないため、ストレスは「輸入物価+3%」（CPI+0.39pt）とした。上限が政策ゼロで吸収できる輸入物価上昇は約3.5%（0.45pt÷0.13）で、円安5%・10%相当は政策なしで超過する。当初の「基準インフレ+0.5pt」も同様に0となるため、余裕0.45ptの内側で意味を持つ「許容インフレ−0.3pt」に置き換えた。15兆円配分例ではエネルギー+20%で15.35兆円、金利+100bpで変化なし。輸入物価は本体に需要反応が無いため、各候補額のCPI経路に既存の為替ストレス（転嫁100%・CPI水準0.13）を重ねて上限内に収まる額を二分探索する。「輸入エネルギー+20%」「借換金利+100bp」も選択できる。何も選ばなければ探索額をそのまま表示する。旧共有URLの `reserve` は破棄し、既定のストレス条件へ移行した旨を表示する。
